import React from 'react';
import PropTypes from 'prop-types';

const Main = () => {
  const handleClick = () => {};
  return (
    <div onClick={handleClick}>
      mainss
      <input />
    </div>
  );
}

Main.propTypes = {
};

export default Main;
